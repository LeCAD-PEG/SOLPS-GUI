--- qt-everywhere-opensource-src-5.6.1/qtbase/src/network/ssl/qsslcontext_openssl.cpp.orig	2016-05-25 17:46:17.000000000 +0200
+++ qt-everywhere-opensource-src-5.6.1/qtbase/src/network/ssl/qsslcontext_openssl.cpp	2016-07-18 16:06:11.000000000 +0200
@@ -470,7 +470,7 @@
             unsigned char *data = reinterpret_cast<unsigned char *>(m_sessionASN1.data());
             if (!q_i2d_SSL_SESSION(session, &data))
                 qCWarning(lcSsl, "could not store persistent version of SSL session");
-            m_sessionTicketLifeTimeHint = session->tlsext_tick_lifetime_hint;
+            // m_sessionTicketLifeTimeHint = session->tlsext_tick_lifetime_hint;
         }
     }
 
--- qt-everywhere-opensource-src-5.6.1/qtbase/src/network/ssl/qsslsocket_openssl.cpp.orig	2016-05-25 17:46:17.000000000 +0200
+++ qt-everywhere-opensource-src-5.6.1/qtbase/src/network/ssl/qsslsocket_openssl.cpp	2016-07-18 16:10:43.000000000 +0200
@@ -391,10 +391,12 @@
             && !(configuration.sslOptions & QSsl::SslOptionDisableServerNameIndication)) {
             // We don't send the trailing dot from the host header if present see
             // https://tools.ietf.org/html/rfc6066#section-3
+#ifdef SSL_CTRL_SET_TLSEXT_HOSTNAME
             if (ace.endsWith('.'))
                 ace.chop(1);
             if (!q_SSL_ctrl(ssl, SSL_CTRL_SET_TLSEXT_HOSTNAME, TLSEXT_NAMETYPE_host_name, ace.data()))
                 qCWarning(lcSsl, "could not set SSL_CTRL_SET_TLSEXT_HOSTNAME, Server Name Indication disabled");
+#endif
         }
     }
 
@@ -1557,9 +1559,10 @@
     // if we have a max read buffer size, reset the plain socket's to match
     if (readBufferMaxSize)
         plainSocket->setReadBufferSize(readBufferMaxSize);
-
+#ifdef SSL_CTRL_GET_SESSION_REUSED
     if (q_SSL_ctrl((ssl), SSL_CTRL_GET_SESSION_REUSED, 0, NULL))
         configuration.peerSessionShared = true;
+#endif    
 
 #ifdef QT_DECRYPT_SSL_TRAFFIC
     if (ssl->session && ssl->s3) {
